# ПЛАН-ДЕЙСТВИЙ.md — Объединённый план: ссылка → работает

> **Источники:** `ПЛАН-ДЕПЛОЯ.md` (обновлён 04.08.2026) + `ПРЕМОРТЕМ-5-ПРИЧИН-ПРОВАЛА.md` (риски).
> **Принцип:** просто «ссылка → открыл → видишь сайт → делаешь заказ». Без лишнего.

---

## ✅ Что уже сделано (Фаза 0 — проверка готовности)

- [x] `npm run build` — сборка без ошибок
- [x] `dist/index.html` — 266 КБ, JS/CSS инлайнены
- [x] `dist/images/` — 7 фото (копируются отдельно, НЕ инлайнены!)
- [x] Git + Node работают

---

## 🔴 КРИТИЧЕСКИЕ БЛОКЕРЫ — помнить при деплое

| # | Проблема (из премортема) | Как избежать |
|---|--------------------------|--------------|
| 1 | **localStorage** — заказы видны только в одном браузере | Решается Фазой 2 (VK-бот + Supabase) |
| 2 | **Картинки не инлайнены** — если закинуть только index.html, фото сломаются | Деплоить ВСЮ папку `dist/`; проверить фото на телефоне |
| 3 | **Контакты-заглушки** | Заменить в Фазе 3 ДО публикации ссылки |
| 4 | **Привычка к ручному деплою** | Vercel: «запушил = задеплоилось» — после `git push` проверять Vercel Dashboard |

---

## ✅ Фаза 1 — Ссылка в интернете ✅ ВЫПОЛНЕНО

- [x] Регистрация на Vercel
- [x] Репозиторий: `unicompact-stack/kenesh-farm`
- [x] Код запушилен
- [x] Задеплоен
- [x] **Ссылка: https://kenesh-farm.vercel.app**
- [x] **Админка: еще проверь сразу **
- [x] Картинки на деплое грузятся (проверено curl, HTTP 200)

## ✅ Фаза 1.5 — Исправления мобильной версии ✅ ВЫПОЛНЕНО

> Пользователь жаловался: «мобильная версия — другой сайт».
> **Выяснилось:** разные фон/картинки на телефоне — задумка по plan-у (карточка-приложение поверх размытого пастбища). Но найдены реальные баги:

- [x] **Опечатка в title**: «КФХ Кинеш» → «КФХ Кенеш — Натуральные молочные продукты» (`index.html`)
- [x] **Кнопки в карточке товара разъезжались** на узких экранах → цена на всю ширину, кнопка «В корзину» на всю ширину (`index.css`)
- [x] **Блок заказа** на очень узких экранах (<380px) → строка «Сумма + инпуты» складывается вертикально (`index.css`)
- [x] Запушено: коммит `2348357` → Vercel пересобрал автоматически
- [x] Проверено: title на проде — «КФХ Кенеш — Натуральные молочные продукты»
- [x] **Админка на мобильном:** шапка (текст кнопок скрыт → иконки), карточки товаров (flex-wrap), карточки заказов (flex-wrap), предпросмотр (flex-wrap) — коммит `bde094f`
- [x] **Шапка на телефоне — единая картинка** `farm-hero.jpg` (было `header-fresh.jpg`) — коммит `fbec375`
- [x] **Бейджи статусов на карточках товаров**: 🟢 «В наличии», 🟡 «Скоро будет», ⚪ «Нет в наличии» — видно сразу, что в наличии, а что нет — коммит `fbec375`

---

## ✅ Фаза 2 — База данных + 152-ФЗ ✅ ВЫПОЛНЕНО

> Решает главную проблему localStorage (премортем №1).
> **Важно:** соблюдаем закон о персональных данных (152-ФЗ)

### Шаг 1: База данных (Supabase) ✅
- [x] Таблица `kenesh_orders` создана
- [x] RLS политика настроена
- [x] REST API работает (чтение/запись)

### Шаг 2: Согласие на обработку ПДн (152-ФЗ) ✅
- [x] Чекбокс добавлен (мобильная + десктопная версия)
- [x] Проверка consent при отправке заказа

### Шаг 3: Политика конфиденциальности ✅
- [x] Страница `#privacy` создана
- [x] Описание: что храним, зачем, не передаём

### Шаг 4: Код обновлён ✅
- [x] `src/lib/supabase.ts` — конфиг
- [x] `src/api/orders.ts` — API (loadOrders, saveOrder, updateOrderStatus)
- [x] `src/App.tsx` — заказы из Supabase, consent,隐私政策
- [x] `src/components/AdminPanel.tsx` — читает из Supabase
- [x] Запушено на GitHub → Vercel задеплоит автоматически

**Почему Supabase:**
- Тот же аккаунт что BossYoki (не нужна новая регистрация)
- PostgreSQL — надёжно
- EU West — соответствует 152-ФЗ (данные в ЕС, не в США)
- Бесплатно до 500 МБ

### Шаг 2: Согласие на обработку ПДн (152-ФЗ)

В `src/App.tsx` добавить **чекбокс** перед кнопкой «Оформить заказ»:

```tsx
// Чекбокс согласия
const [consent, setConsent] = useState(false);

// В форме заказа:
<label className="flex items-center gap-2 text-sm text-gray-600">
  <input
    type="checkbox"
    checked={consent}
    onChange={(e) => setConsent(e.target.checked)}
    className="w-4 h-4"
  />
  <span>
    Я согласен на обработку персональных данных
    (<a href="/privacy" className="underline">политика</a>)
  </span>
</label>

// В handleSubmit проверять:
if (!consent) { alert('Нужно согласие на обработку данных'); return; }
```

### Шаг 3: Политика конфиденциальности

Создать файл `src/pages/PrivacyPolicy.tsx` или секцию в `App.tsx`:

```
ПОЛИТИКА КОНФИДЕНЦИАЛЬНОСТИ

1. Какие данные собираем:
   - Имя (если указываете)
   - Телефон
   - Адрес доставки

2. Зачем:
   - Для доставки заказа
   - Для связи по заказу

3. Не передаём:
   - Третьим лицам
   - В рекламные сети
   - На другие сайты

4. Хранилище:
   - Supabase (ЕС, шифрование)
   - Удаляем по запросу

5. Контакт:
   - info@kenesh-farm.ru
```

Ссылка: `https://kenesh-farm.vercel.app/#privacy` или отдельная страница.

### Шаг 4: VK-уведомления через BossYoki ✅ ВЫПОЛНЕНО
> Добавлено в `smart_bot.py` (v2.4.0), задеплоено на Render
- [x] `check_dairy_orders()` в BossYoki/smart_bot.py — проверяет каждые 30 сек новые заказы (`status = 'new' AND notified = FALSE`)
- [x] Запущен в фоне: `threading.Thread(target=check_dairy_orders, daemon=True).start()`
- [x] Колонка `notified` в таблице `kenesh_orders` (добавляется автоматически при старте бота)
- [x] Протестировано: тестовый заказ → VK-уведомление → `notified=true` (заказ #4, 04.08.2026)

### Шаг 5: Связать всё ✅
- [x] Сайт отправляет заказ → Supabase
- [x] Админка читает из Supabase
- [x] BossYoki проверяет Supabase → VK-уведомление

---

## 🟢 Фаза 3 — Контакты (полдня)

- [ ] Узнать реальные контакты фермы
- [ ] Телефон → реальный (сейчас `+7 (906) 023-64-64`)
- [ ] Email → реальный (сейчас `kfh-kenesh@yandex.ru`)
- [ ] VK → ссылка на группу (сейчас `href="#"`)

---

## ⚠️ Чек-лист перед публикацией ссылки

- [x] Картинки грузятся на телефоне
- [ ] Телефон/email/VK — реальные
- [x] Заказ сохраняется в Supabase (не localStorage)
- [x] Админка работает (`/#admin`)
- [x] Ссылка `https://kenesh-farm.vercel.app` открывается

---

## ✅ Выполнено (сводка)

| Фаза | Что | Статус |
|------|-----|--------|
| 0 | Сборка | ✅ |
| 1 | Vercel деплой | ✅ |
| 1.5 | Мобильная версия | ✅ |
| 2 | Supabase (товары + заказы) | ✅ |
| 2 | VK-уведомления | ✅ |
| 3 | Контакты | ⏳ |

## ⚡ Шпаргалка «запушил = задеплоилось»

```bash
cd "молочные продукты/adaptive-dairy-delivery-interface"
export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npm run build
git add .
git commit -m "Описание изменений"
git push
# → Vercel сам пересоберёт и задеплоит (1–2 мин)
```

---
*Обновлено: 04.08.2026. Переписано по новому ПЛАН-ДЕПЛОЯ (VK + Supabase вместо Telegram).*