# Рекомендации: деплой проектов на Vercel

---

## 1. Структура документации КФХ Кенеш

| Файл | Что | Статус |
|------|-----|--------|
| `CLAUDE.md` | Основная документация (стек, структура, деплой) | **ОСТАВИТЬ** |
| `ПЛАН-ДЕЙСТВИЙ.md` | Рабочий план (фазы, чек-листы) | **ОСТАВИТЬ** |
| `buyer-app-plan.md` | Оригинальный план (история) | **ОСТАВИТЬ** |
| `ПРЕМОРТЕМ-5-ПРИЧИН-ПРОВАЛА.md` | Анализ рисков (история) | **ОСТАВИТЬ** |
| `РЕКОМЕНДАЦИИ.md` | Этот файл | **ОСТАВИТЬ** |

---

## 2. Как деплоить другие проекты на Vercel

### Общий принцип (одинаковый для всех):

```
GitHub репозиторий → Vercel → ссылка .vercel.app
```

### Пошагово для любого проекта:

**Шаг 1: Проверить есть ли `package.json`**
```bash
ls package.json
```
- Если **есть** → проект на React/Vue/Next.js → деплоим как КФХ Кенеш
- Если **нет** (Python, статика) → другой способ (см. ниже)

**Шаг 2: Создать GitHub репозиторий**
```bash
git init
git add .
git commit -m "Первая версия"
git remote add origin https://github.com/unicompact-stack/ИМЯ-ПРОЕКТА.git
git push -u origin main
```

**Шаг 3: Подключить к Vercel**
1. https://vercel.com → "Add New" → "Project"
2. Выбрать репозиторий
3. Настройки (обычно Vercel определяет сам):
   - Framework: Vite / Next.js / Create React App
   - Build Command: `npm run build`
   - Output Directory: `dist` / `build` / `.next`
4. "Deploy"

**Шаг 4: Готово!**
Ссылка: `https://ИМЯ-ПРОЕКТА.vercel.app`

---

## 3. Какие проекты можно деплоить

| Проект | Тип | Можно на Vercel? | Как |
|--------|-----|------------------|-----|
| **КФХ Кенеш** | React + Vite | ✅ Уже задеплоен | Готово |
| **BossYoki** | Python + VK API | ❌ | Нужен сервер (Render) |
| **bot-novichkam** | Python + VK API | ❌ | Нужен сервер (Render) |
| **avito-zapolnatel** | Python + Selenium | ❌ | Нужен сервер (Render) |
| **Dashboard** | HTML/JS | ✅ | Закинуть dist/ |
| **Любой лендинг** | HTML/CSS/JS | ✅ | Закинуть dist/ |

### Правило:
- **Есть `package.json`** → Vercel (бесплатно, авто-деплой)
- **Python / боты / сервер** → Render (бесплатно, 24/7)

---

## 4. Быстрый деплой нового React-проекта

Если проект уже собран и есть `dist/`:

1. Создай GitHub репозиторий
2. Запушь код
3. Vercel → "Add New" → Project → выбери репозиторий
4. **Важно:** укажи Output Directory = `dist`
5. Deploy

Готово за 5 минут.

---

## 5. Если проект НЕ на React (простой HTML)

Можно просто:
1. Закинуть папку `dist/` через drag-and-drop на Vercel
2. Или запушить на GitHub Pages (бесплатно)

---

*Создано: 04.08.2026*
